# 参考数据

- get-task-priority-params.dto 定义了code的格式
- task-priority.entity 定义了任务优先级数据库实体

- reference-data.module 模块入口，仅仅多导入了数据库模块
- task-priority.service 检索任务的服务
- reference-data.controller 控制器，提供了两个检索接口
